# No API key needed — generate_resume.py calls the Claude Code CLI, which
# uses your logged-in Claude subscription. Run `claude /login` once if you
# haven't already.

# Optional: override the default model (sonnet). Accepts a CLI alias
# (sonnet, opus, haiku, fable) or a full model name.
# CLAUDE_MODEL=sonnet
